#include <stdio.h>                 


int main()
{   
	int size_of_a_double_variable = sizeof(double);

	printf("Size of a double variable %d bytes.", size_of_a_double_variable);

    return 0;                      
}