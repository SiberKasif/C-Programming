#include <stdio.h>                 


int main()
{   
    
    int size_of_floats = sizeof(float);
    
	printf("Size of a float variable is %d bytes.", size_of_floats);

    return 0;                      
}

